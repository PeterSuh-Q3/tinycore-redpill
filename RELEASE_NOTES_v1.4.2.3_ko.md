
# alpine-redpill v1.4.2.3

NVIDIA 하드웨어 트랜스코딩 지원과 메뉴 구조 개편을 중심으로 한 릴리즈입니다.

## NVIDIA 하드웨어 트랜스코딩 전 구간 지원

무인증 NVIDIA 드라이버(물리 / passthrough GPU 전용 — vGPU·라이선스 서버 없음)를 메인 메뉴 `g`에서 바로 선택할 수 있습니다. 드라이버 버전을 고르고, 필요하면 Jellyfin 패키지용 NVENC ffmpeg를 함께 추가한 뒤 애드온을 활성화하면 로더 빌드가 이를 DSM까지 실어 나릅니다.

서브메뉴는 고정 목록이 아니라 실제 카탈로그에서 항목을 해석하므로, 사용 중인 플랫폼**과** 커널에 실제로 존재하는 드라이버만 표시됩니다:

- **커널 5.10.55** 플랫폼은 네 개 브랜치(470 / 535 / 550 / 580) 전부
- **커널 4.4** 플랫폼은 550만. 이건 패키징 선택이 아니라 NVIDIA가 정한 하한입니다 — 560부터 `nv-linux.h`가 `does not support kernels older than Linux 4.15`로 즉시 중단하며, 560/570/575/580을 실제로 4.4 트리에 빌드해 확인했습니다.
- **커널 3.10** 플랫폼은 메인 메뉴에 `(Not Supported)`로 표기되고 서브메뉴 진입 자체가 막힙니다. 빈 목록을 열어 보여주지 않습니다.

GSP 펌웨어도 함께 판단합니다. 580은 Turing 이상에서 GSP 펌웨어를 요구하는데 NVIDIA의 `.run`에는 Turing과 컨슈머 Ampere용 blob만 들어 있습니다. Ada나 Blackwell 카드에서는 580을 추천 후보에서 조용히 제외하고 550을 가리킵니다 — 그대로 두면 설치는 되지만 초기화에 실패하기 때문입니다.

실기 검증을 마쳤습니다. Quadro P620으로 `epyc7002`(커널 5.10.55)와 `geminilake`(커널 4.4.302) 양쪽에서 Jellyfin 트랜스코딩이 동작합니다.

## 애드온 보존 방식의 일반화

로더 빌드는 `bundled-exts.json`을 upstream 기준으로 초기화하는데, 사용자가 켜둔 애드온은 하드코딩된 캡처 목록에 이름이 있을 때만 그 초기화를 견뎠습니다. 새 애드온마다 보존을 위한 코드를 따로 넣어야 했다는 뜻입니다.

이제는 현재 파일과 새로 받아온 upstream 기본값을 비교(모듈팩 제외)해 `merged-addons.json`을 만들고, 초기화 후 다시 병합합니다. 나중에 추가되는 애드온까지 애드온별 코드 없이 모두 보존됩니다.

## 메뉴 구조 개편

메인 메뉴를 실제 빌드 워크플로 순서에 맞춰 정리하고 계층을 낮췄습니다.

- `k`(로더 모드)와 `c`(DDSML/EUDEV)를 `z` 하위 1·2번 항목으로 이동. 진입 시 로더 모드 선택이 기본 선택됩니다.
- `dtsmapping`을 `n` 하위메뉴에서 `z`의 3번 항목으로 이동.
- `c`(커널 모듈 처리 방법)를 메뉴 맨 앞이 아니라 `p`(로더 빌드) 바로 위에 배치.
- 인덱스 문자를 전부 소문자로 통일 — `N`은 `g`로, `X`는 `o`로. `k`가 비면서 여유가 충분해졌습니다.

MAC 주소 선택은 최대 8개 인터페이스를 지원하는 별도 하위메뉴가 되었고, 실제로 존재하는 NIC 수만큼 `a`부터 `h`까지 순차로 인덱스가 부여됩니다. NIC이 하나뿐인 장비에서는 고를 대상이 없으므로 목록을 건너뛰고 바로 주소 선택 화면으로 들어갑니다.

상단 표시줄의 키맵 표기 자리를 패널 크기로 교체해, 현재 선택된 저장소 패널(예: `RACK_12_Bay`)을 한눈에 확인할 수 있습니다.

## X용 Mesa DRI 드라이버

`apk add` 목록에 `xorg-server`와 `lxterminal`만 있고 Mesa DRI 드라이버가 빠져 `/usr/lib/dri/`가 비어 있었습니다. AIGLX가 `swrast_dri.so`를 찾지 못하고 glamor 초기화도 실패해, X·openbox·tint2·lxterminal 프로세스는 떠 있는데 프레임버퍼가 정지한 상태였습니다 — Proxmox VM에서 screendump를 두 번 캡처해 md5가 동일함을 확인했습니다. `mesa-dri-gallium`, `mesa-gl`, `mesa-egl`, `mesa-gbm`을 추가해 소프트웨어 렌더링 경로를 복구했습니다.

## 다국어

두 개의 문자열을 새로 추가하고 지원하는 18개 언어 전체에 번역을 넣었습니다. MAC 메뉴 제목(최대 8개 지원 명시)과, 선택한 언어와 무관하게 한국어로 고정돼 있던 메뉴 종료 항목입니다.

## 참고

- 기존 v1.4.2.2 사용자는 업데이트하지 않으면 영향이 없습니다.
- NVIDIA 드라이버 애드온은 redpill-load의 모든 확장이 그렇듯 매 부팅마다 레이어를 다시 내려받습니다. 이 애드온은 전송량이 큰 편(ffmpeg 레이어 포함 시 약 420MB)이라 회선이 느린 환경에서는 알아두실 필요가 있습니다.
- 커널 4.4 드라이버 빌드는 vermagic 검사를 통과하고 `geminilake`에서 정상 로드되지만, 헤드리스 4.4 플랫폼(`broadwell*`, `purley`, `r1000`, `v1000`)은 아직 실기 검증 전입니다.

# alpine-redpill v1.4.2.2

안정성 및 일관성 보완 릴리즈입니다.

## 리비전 선택 메뉴가 최고령 버전을 계속 누락하던 문제 수정

`selectversion()`의 `tags=(a..k)` 조회 배열이 지난 릴리즈의 `.[:12]` 슬라이스 수정 이후에도 11개 문자밖에 없어서, 일부 모델에서 12번째(가장 오래된) 리비전의 태그가 비어 메뉴에서 소리 없이 사라지고 있었습니다. 배열을 12개 문자로 확장했습니다.

## BMI2 버전 필터링을 전 구간에서 통일

`all-modules`는 DSM 7.3.0부터 BMI2 지원 CPU를 요구하지만, BMI2 없는 CPU용 대안인 `custom-modules`는 실제로는 DSM 7.3.2까지만 빌드되어 있습니다(시놀로지가 그 이후 버전에 필요한 DSM 7.4 GPL 커널 소스를 아직 공개하지 않음). 기존에는 버전 선택 메뉴만 이 경계를 반영했고, 모듈명/로더모드 선택 메뉴는 "DSM >= 7.3.0"이라는 느슨한 조건만 확인해 실제로는 존재하지 않는 DSM 7.4.0+에서도 `custom-modules`를 제공하는 경우가 있었습니다.

이제 세 선택 메뉴 모두 동일한 명명된 임계값을 공유해, BMI2 없는 CPU에서는 어디서든 일관되게 `< 7.3.0`(all-modules) 또는 `7.3.0~7.3.2`(custom-modules)만 사용 가능하도록 노출됩니다. 저장된 DSM 버전이 7.3.2를 초과한 상태로 남아있어도, 버전 선택 단계까지 가지 않고 BMI2 없는 모델을 선택하는 즉시 자동으로 보정됩니다.

## alpine-redpill 전용 /etc/motd

포크 이전 시절의 stale `main` 브랜치 경로에서 받아오던 TinyCore 시절 `/etc/motd`를 컬러풀한 alpine-redpill 로고로 교체했습니다. 이제 apkovl 빌드 시점에 직접 구워져 들어가므로, `menu.sh`를 한 번 실행한 이후가 아니라 최초 부팅부터 바로 보입니다. mshell에만 적용되며, xTCRP에는 `/etc/motd` 메커니즘 자체가 없어 영향이 없습니다.

## 참고

- 기존 v1.4.2.1 사용자는 직접 업데이트하지 않는 한 영향받지 않습니다.
